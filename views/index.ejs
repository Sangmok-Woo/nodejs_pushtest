<!DOCTYPE html>
<html>
<head>
  <title>게시판</title>
</head>
<body>
  <h1>📋 게시판 목록</h1>
  <a href="/write">글 쓰러 가기 🖊️</a>
  <ul>
    <% posts.forEach(post => { %>
      <li><a href="/view/<%= post.id %>"><%= post.title %></a> | <%= post.created_at %></li>
    <% }); %>
  </ul>
</body>
</html>
